# GTAVR — Reprise 2026-07-26 (soir)

## État validé EN JEU (casque Pimax, utilisateur)

- **Caméra scriptée VR fonctionnelle** : GTAVRBridge.asi (ScriptHookV) +
  VRCamera dans OVRInject. Le jeu rend via notre caméra, parentée à la cam
  gameplay. Head tracking + stéréo AER confirmés au casque par l'utilisateur.
- Réglages overlay actifs : Stereo Offset X/Y (±30/±15, 1 u = 1% UV), FOV
  override (global/par type), offsets caméra X/Y/Z, world scale, IPD,
  renderScale. Les réglages "depth" (reprojection) ne s'appliquent qu'au
  mode Z3D (2D), pas en AER — par design.

## Fixes du soir (commites)

- `41c3556` bridge + client + VRCamera initial.
- `c643f99` **réécriture complète du delta math** (les axes étaient
  mélangés : yaw→pitch etc.) : D_local = H·R⁻¹, remap axes XR→cam via J,
  application en espace LOCAL (B' = D_cam·B). Vérifié par
  `build_int/verify_vrcamera_math.py` (ALL PASS — round-trip Euler exact,
  yaw/pitch/roll sur le bon axe, angle monde = angle tête pour toute base).
- `1fad08e` **throttle XR quand le casque dort** : session SYNCHRONIZED/
  VISIBLE → Pimax throttle xrWaitFrame → étouffait le render thread du jeu
  (chargement 25+ min). Pompe à 10 Hz dans ces états. + retry bridge 5 s.

## BLOQUANT ACTUEL : le jeu ne charge plus (environnement, pas le mod)

- Depuis ~15:20, TOUT boot de GTA V stagne ~10-25 min dans le slideshow de
  chargement (RSS figé à ~1031 MB, CPU qui spinne, 0 I/O disque), même SANS
  injection du mod (A/B prouvé), même après kill de toute la stack Rockstar.
- La phase 1031 MB finit par se débloquer (une fois : saut à 3,8 GB au bout
  de ~10-15 min), puis chargement story encore lent (~10 min de plus).
- Suspects environnement : sdk_legacy_led_x64.dll (Logitech, injecté dans
  GTA5, connexion loopback:9010 ↔ lghub_agent), stack Social Club (plusieurs
  SocialClubHelper zombies trouvés), simple dégradation après ~6 taskkill
  pendant le chargement. **Recommandation : rebooter le PC.**
- UAC nécessaire pour renommer dinput8.dll (test A/B propre) — demander à
  l'utilisateur.

## À faire au retour de l'utilisateur

1. Reboot PC (ou au moins relancer launcher + jeu), boot normal.
2. Injecter (throttle fix inclus → chargement rapide même casque posé).
3. Retest casque : yaw/pitch/roll dans le bon sens (nouvelle math),
   recenter view, superposition via Stereo Offset, FOV override.
4. Si un axe est encore inversé : fix d'une ligne (le round-trip est
   auto-cohérent, seul le signe d'un axe peut rester faux).

## Soir 21h — tout est validé au casque

- **Axes OK** (confirmé utilisateur), XR-FOV cam (103,3°) + crop angulaire :
  image naturelle plein cadre (vérifié sur dumps F12 : disparité stéréo
  réelle). Artefacts nettoyés (clear RT).
- **IPD auto** (`57a4c67`) : offset œil = pose runtime (IPD physique HMD),
  checkbox overlay, persisté. Manuel = slider.
- **Résolution** : forcer ResizeBuffers de l'extérieur = refusé par DXGI
  (INVALID_CALL). Leviers réels : réglages GTA (Résolution + Frame Scaling)
  ou shim dxgi + override à la création (UAC requis, futur).
- **F12** = dump backbuffer + yeux dans %TEMP%\gtavr_dump.
- Astuce input : pour piloter le jeu à distance, cliquer la BARRE DE TITRE
  (focus sans toucher l'UI) puis keybd_event — le clic dans le jeu ouvre la
  carte. PostMessage marche aussi (parfois).

## Rappels

- Ne JAMAIS injecter ScriptHookV.dll à distance (crash init) — il passe par
  la chaîne dinput8 du dossier jeu.
- NativeTrainer.asi tue le boot — ne pas le réinstaller.
- Bridge : Vector3 natifs stride 8 (x@0,y@8,z@16) — types.h du SDK.
- Log mod : %TEMP%/gtavrInjectLog.txt (84M+ lignes — purger un jour).
